import {HeaderWrapper, HeaderContainer, HeaderTitle} from './header.items';

const Header = () => {
	return (
		<HeaderWrapper>
			<HeaderContainer>
				<HeaderTitle>Slider Component App</HeaderTitle>
			</HeaderContainer>
		</HeaderWrapper>
	);
};

export default Header;
