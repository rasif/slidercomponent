import {MainWrapper, MainContainer} from './main.items';
import {Slider} from '../';

const Main = () => {
	return (
		<MainWrapper>
			<MainContainer>
				<Slider />
			</MainContainer>
		</MainWrapper>
	);
};

export default Main;
